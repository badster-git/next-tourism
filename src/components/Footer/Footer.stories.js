import  {Footer} from "./Footer";

export default {
  title: "Brand/Footer",
  component: Footer,
};

const Template = (args) => (
  <Footer {...args}></Footer>
);
export const Primary = Template.bind({});
