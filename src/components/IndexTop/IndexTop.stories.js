import { IndexTop } from "./IndexTop";

export default {
  title: "Brand/Index Top",
  component: IndexTop,
};

const Template = (args) => <IndexTop {...args}>Default Header</IndexTop>;
export const Primary = Template.bind({});
